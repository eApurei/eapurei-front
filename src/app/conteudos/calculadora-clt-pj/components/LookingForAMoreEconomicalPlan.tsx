import { roboto } from '@/lib/fonts'

export default function LookingForAMoreEconomicalPlan() {
  return (
    <main
      className={`${roboto.className} mt-12 flex w-full flex-col items-center justify-center gap-4 bg-slate-300 p-12 dark:bg-grey-100 lg_2:px-2`}
    >
      <section className="flex items-center justify-center bg-yellow-300">
        oi
      </section>
    </main>
  )
}
